const API = "http://localhost:1337";
const GETTERS = {
    categories: `${API}/categories`,
    products: `${API}/products`,
    pages: `${API}/pages`,
    socials: `${API}/socials`,
}

export { API, GETTERS };
